# AdressFilter-WithRegex
## To use this projectyou will need to install a Library called csv-parser
### Get inside the folder of the project with the command prompt and write : npm install csv-parser
